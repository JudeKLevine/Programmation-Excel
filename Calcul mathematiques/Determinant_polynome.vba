Function Delta(ByVal a As Double, ByVal b As Double, ByVal c As Double) As Double

    'ici le polynome est sous la forme
    'ax² + bx + c, on applique juste la formule
     Delta = b * b - 4 * a * c
     
End Function
